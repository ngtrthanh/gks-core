# D8 — Bounded Completeness Proof: Six End-to-End Compile Runs

**Project:** Governance Computing — Phase 0: Governance Kernel Discovery **Author:** Guild A — Governance Computing Researchers **Status:** Draft v0.1 — submitted to Agent 0 (companion: coverage-matrix.md) **Binding frame:** Ratified Constitution (D0). ⟨B₁, T₁⟩ FROZEN. **Iron Rule in force:** any unrepresentable mechanism aborts its run and logs a Falsification Event with structural post-mortem. No patches were applied in this exercise; one *reformulation strain* was encountered and is logged (Run 4, §4.5) — the distinction between reformulation-within-the-frozen-kernel (lawful) and amendment (forbidden) is defined in §0.

**Notation.** Kernel instances carry TIX as SSA-style subscripts: `id⟨τₜ, τ_f⟩` where τₜ \= text-validity interval, τ\_f \= fact-application interval (∞ \= open). Source-map entries: `⟦doc : locus : kind⟧`. Registry tokens in `small-caps`. E-layer content is stated as transition triggers and mutation hooks against the frozen alphabet 𝒜 \= {proposed, in-force, suspended, violated, discharged, extinguished}. Verdicts: **COMPILED** (total), **COMPILED-C** (total, with open-texture conditional tokens per D0 §10), **ABORT/FE** (falsification event — none occurred).

**§0 Iron-Rule adjudication standard.** *Reformulation* \= re-expressing a clause's semantics in different frozen-kernel terms after a first encoding fails (lawful; logged as STRAIN). *Amendment* \= adding a constructor, T-operation, sort, E-state, or changing a signature (forbidden; \= FE). The audit trail below preserves failed first encodings where they occurred.

**Source-text handling.** US statutory text is quoted (public domain). ISO and FIDIC materials are structure-preserving paraphrases (licensed texts; \[Unverified: paraphrase fidelity against current editions — flagged for the licensed-corpus pass of Track A\]); the SOP, JD, and KPI artifacts are the program's synthesized benchmarks, composited from common industrial forms.

---

## Run 1 — Statutory Article: 26 U.S.C. §121 (excerpt)

**1\. Source text (quoted, public domain).**

(a) Gross income shall not include gain from the sale or exchange of property if, during the 5-year period ending on the date of the sale or exchange, such property has been owned and used by the taxpayer as the taxpayer's principal residence for periods aggregating 2 years or more. … (b)(3) Subsection (a) shall not apply to any sale or exchange by the taxpayer if, during the 2-year period ending on the date of such sale or exchange, there was any other sale or exchange by the taxpayer to which subsection (a) applied.

**2\. Layer Σ.** `n1⟨τₜ=[current-code,∞), τ_f=∞⟩` \= NRM(bearer=`irs`, counterparty=`taxpayer`, act=include-in-gross-income(gain(sale)), sign=−, force=`mandatory`) — the exclusion as a prohibition on inclusion. CLS: `c1` \= CLS(property, `principal-residence`, ctx=n1) — note: "principal residence" is *defined elsewhere in the code*; compiled as classification with REF into the definitional locus, not as open texture.

**3\. Layer X.** No PWR content in the excerpt (no delegation/authority). E-machines instantiate n1 per taxpayer-sale event; verdict evaluation is π₃ over the sale event trace.

**4\. Layer K.** `g1` \= GRD(condition \= \[owned(p) ∧ used-as(c1)\] for aggregate ≥ 2y within window(5y, ending=sale-date), body \= n1, priority=`base`) — T₁: windowed aggregation, native. `g2` \= GRD(condition \= count(sales s′: applied(n1, s′), window(2y, ending=sale-date)) ≥ 1, body \= ¬applicable(n1), priority=`override`, defeats=\[REF(g1)\]) — the (b)(3) anti-stacking exception: **an exception whose condition quantifies over the prior application history of the very rule it defeats.** T₁ handles it because `applied(n1, s′)` is a read-only E-history predicate (sort 6\) under bounded window counting — exactly the machinery D6/A4 forced into Definition 4\.

**5\. TIX.** All instances inherit the section's current-code τₜ; τ\_f unbounded; a future amendment would fork `n1⟨τₜ′⟩` with the source-map preserving both.

**6\. Layer P source-map.** `n1 → ⟦usc-26 : §121(a) : subsection⟧`, `g1 → ⟦usc-26 : §121(a) : condition-span⟧`, `g2 → ⟦usc-26 : §121(b)(3) : subsection⟧`.

**Verdict: COMPILED.** The self-referential application-history exception — the run's designed kill-shot — lands inside frozen T₁.

> **Encoding note (exit-review M2, corrected 2026-07-10).** The narrative above
> writes `g2 … defeats=[REF(g1)]` (an exception defeating the *guard* g1 —
> guard-vs-guard, as in the idealized D1.4 S-Defeat over guard target-sets). The
> **executable encoding differs**: `ingest_benchmark` sets `g2.Defeats = [n1]`
> (g2 defeats the *norm* directly), because the single-pass resolver
> (`internal/evaluator.Resolve`) matches guards against the norm ID only and does
> **not** implement guard-vs-guard defeat chains. The *outcome* is identical for
> this run (n1 is suppressed when the prior-sale window is met), but the mechanism
> is a higher-priority guard-vs-norm defeat, not exception-to-exception. The D1.4
> S-Defeat guard-vs-guard form is unimplemented (tracked in PROGRESS WS-E); this
> note records the divergence rather than hiding it.

---

## Run 2 — ISO Compliance Chapter: ISO 9001:2015 §8.7 (structure-preserving paraphrase)

**1\. Source text (paraphrase).** *Outputs not conforming to requirements must be identified and controlled to prevent unintended use or delivery. The organization must take appropriate action based on the nature of the nonconformity and its effect; conformity must be re-verified when nonconforming outputs are corrected; documented information recording the nonconformity, actions taken, concessions obtained, and the deciding authority must be retained.*

**2\. Layer Σ.** `n2a` \= NRM(`org`, `cert-body`, act=identify∧control(output: c2), \+, `mandatory`); `c2` \= CLS(output, `nonconforming`, ctx=REF(`iso9001-8.6-requirements`)); `n2b` \= NRM(`org`, `cert-body`, act=take-action(`appropriate`), \+) — **`appropriate` compiles as open-texture token OT-1**; `n2c` \= NRM(`org`, `cert-body`, act=re-verify(output), \+); `n2d` \= NRM(`org`, `cert-body`, act=retain(documented-info), \+). CLS: `c3` \= CLS(person, `concession-authority`, ctx=8.7) — the deciding-authority record presupposes an authority classification.

**3\. Layer X.** Concession is a live PWR: `p1` \= PWR(holder=REF(c3), operand=⟨GRD suspending n-conformity for designated output-lot⟩, effect=`create`, exercise=`concession-record`) — a concession *is* a bounded, evidenced waiver-power. Its exercise transitions the affected requirement-instance to `suspended` in E with mutation hook target-rebind (the concession's scope), and n2d's retention duty attaches to the exercise event itself.

**4\. Layer K.** `g3` \= GRD(condition=corrected(output), body=n2c) — re-verification triggered by correction. REF lattice: n2a→`8.6` (mode=`derives-from`); the whole clause REF(mode=`satisfies`)-targetable by SOPs (Run 3). VAL: none native to 8.7 — correct: the clause is deliberately qualitative; quantification arrives via org KPIs (Run 6), which is the layering working as designed.

**5\. TIX.** τₜ \= \[2015-edition, next-edition); an organization's transition period between editions is two coexisting τₜ forks — the AKN-benchmark case, native.

**6\. Source-map.** `n2a…n2d → ⟦iso9001 : 8.7.1/8.7.2 : clause⟧`; `p1 → ⟦iso9001 : 8.7.1(c)-concession : inline⟧`.

**Verdict: COMPILED-C** (one boundary token OT-1, per D0 detect/contain: verdicts touching "appropriate action" emit conditional).

---

## Run 3 — Enterprise SOP: Incident-Response SOP-07, step 4 (synthesized tri-source benchmark)

**1\. Source text.** *Step 4: The incident handler shall log every security incident in the register within 24 hours of detection (per ISO/IEC 27001 control A.5.24 \[paraphrase-level reference\] and Decree 13/2023/NĐ-CP Art. 26 \[Unverified: article number not re-verified\]); after two consecutive logging failures in any quarter, the handler's logging duty escalates to the Security Manager, who may authorize delegated logging by the on-call analyst.*

**2\. Layer Σ.** `n3` \= NRM(`incident-handler-role`, `org`, act=log(incident, register), \+, `mandatory`); role occupancy: `c4` \= CLS(person-REF, `incident-handler`, ctx=`org-chart@τ`).

**3\. Layer X.** `p2` \= PWR(holder=`security-manager`, operand=⟨NRM schema: log-duty over `on-call-analyst`⟩, effect=`create`, exercise=`written-authorization`) — the delegation arm. Escalation transition: on trigger (see g4) the E-machine of n3 transitions bearer-binding via mutation hook target-rebind(`security-manager`) — **note the audit point: bearer-rebinding is an E-mutation of a K̂ binding, lawful because it executes a GRD/PWR configuration declared in K̂, not an ad-hoc write.**

**4\. Layer K.** `g4` \= GRD(condition \= count(violated(n3-instance), window(quarter)) ≥ 2 ∧ consecutive, body \= escalation-config, priority=`override`, defeats=\[REF(n3.bearer-binding)\]). REF: `r1` \= REF(n3, `iso27001-A.5.24`, mode=`satisfies`); `r2` \= REF(n3, `decree-13-2023-art26`, mode=`satisfies`). VAL: `v1` \= VAL(fn=elapsed(detection→log-entry), unit=`hours`, comparator=≤, target=24) bound into n3's act term. **5\. TIX.** `r2⟨τₜ=[decree-in-force,∞), τ_f=[2023-07-01,∞)⟩` \[Unverified: decree effective date\] — if the decree is later amended, r2 forks and π₃ reports the SOP's compliance status *per interval*: the Monday Morning query, executed. **6\. Source-map.** `n3 → ⟦sop-07 : step-4 : step⟧`; `g4 → ⟦sop-07 : step-4-escalation : block⟧`; `v1 → ⟦sop-07 : step-4-timing : inline⟧`.

**Verdict: COMPILED.** The full tri-source pattern — D2's E4 killer — compiles with both satisfaction edges, the metric binding, and a two-stage authority structure.

---

## Run 4 — Procurement/EPC Contract Clause: Engineer delegation \+ remedies (FIDIC-3.2-pattern paraphrase \+ LD clause)

**1\. Source text (paraphrase).** *(i) The Engineer may from time to time assign duties and delegate authority to assistants, except the authority to make Determinations, unless otherwise agreed by the Parties; acts of an assistant within a delegation have the same effect as acts of the Engineer. (ii) For delay, the Contractor shall pay liquidated damages of 0.05% of the Contract Price per day, capped at 10%; such payment shall be the Employer's sole and exclusive remedy for delay.*

**2\. Layer Σ.** Part (i): as decomposed in D3/P6 — the five-element structure. `c5` \= CLS(act(assistant), act(engineer), ctx=delegation-instrument) — attribution. Part (ii): `n4` \= NRM(`contractor`, `employer`, act=pay(v2), \+); `n5` \= NRM(`employer`, `contractor`, act=claim(remedy-for-delay other than n4), −, `mandatory`) — see §4.5.

**3\. Layer X.** `p3` \= PWR(engineer, operand=⟨duty/authority schema ∖ `determinations`⟩, effect=`create`, exercise=`notice`); `p4` \= PWR(`parties-jointly`, operand=⟨the carve-out GRD⟩, effect=`extinguish`, exercise=`agreement`) — third-order unlock. E: delegation instances instantiate per notice; LD accrual is an E-evaluation over the delay trace with `discharged` on payment.

**4\. Layer K.** Carve-out: `g5` \= GRD(priority=`high`, defeats=\[REF(p3.operand)\], body=exclusion(`determinations`)); qualification precondition `g6` \= GRD(condition=CLS(assistant,`qualified`), body=p3). VAL: `v2` \= VAL(fn=min(0.0005 × contract-price × delay-days, 0.10 × contract-price), unit=`currency`, comparator==, target=computed) — cap arithmetic native to T₁.

**§4.5 STRAIN LOG (Iron-Rule audit trail).** First encoding of "sole and exclusive remedy" attempted GRD(defeats \= ⟨all other remedy-norms, present and future⟩) — **fails**: `defeats` takes REF\* (enumerable targets), and the clause quantifies over an *open class* of norms. Amendment of `defeats` to accept schemas would be a kernel signature change \= **forbidden**. Reformulation found within frozen B₁: the exclusivity is not a priority relation but a *prohibition on claiming* — `n5` above, whose act-term carries a Schema⟨n⟩ over remedy-norm instances (T₁ sort 7, stratified). Semantic audit: the reformulation captures enforcement reality (a claim outside the LD regime is a breach of n5) and loses nothing testable \[Inference — flagged for adversarial legal review in Track A\]. **Verdict on strain: resolved without amendment; logged, not hidden.**

**5\. TIX.** Delegations: `p3-instances⟨τₜ=[notice, revocation)⟩`; LD accrual τ\_f per delay-day. **6\. Source-map.** `p3,g5,c5 → ⟦contract : sub-clause-3.2 : clause⟧`; `n4,v2,n5 → ⟦contract : clause-8.7-LD : clause⟧`.

**Verdict: COMPILED** (one strain, resolved; zero amendments).

---

## Run 5 — Job Description: Quality Manager (synthesized composite)

**1\. Source text.** *Reports to the Operations Director. Responsibilities: maintain the QMS in conformity with ISO 9001; approve or reject concession requests up to $50,000 in impact; stop production upon detection of critical nonconformity. Competencies: ISO 9001 lead-auditor certification. Performs other duties as assigned by the Operations Director.*

**2\. Layer Σ.** `c6` \= CLS(person, `quality-manager`, ctx=org-chart); `n6` \= NRM(`quality-manager`, `org`, act=maintain(qms), \+) with REF(mode=`satisfies`, →`iso9001`); `c7` \= CLS(person, `lead-auditor-certified`, ctx=hiring) — competency as certified classification (D3 pattern).

**3\. Layer X.** Authorities compile as powers, the JD's defining insight: `p5` \= PWR(`quality-manager`, operand=⟨concession-GRD schema⟩, effect=`create`, exercise=`approval`) bounded by `g7` \= GRD(condition \= VAL(impact, `usd`, ≤, 50000), body=p5); `p6` \= PWR(`quality-manager`, operand=⟨production-line NRM: operate → suspended⟩, effect=`modify`, exercise=`stop-order`) — stop-authority as a suspension-power over operational norms, executing on E's frozen alphabet. **"Other duties as assigned"**: `p7` \= PWR(`operations-director`, operand=⟨NRM schema: bearer=`quality-manager`, act ∈ ℛ\_act(role-family)⟩, effect=`create`, exercise=`assignment`) — the classic JD catch-all is *not* open texture; it is an explicit duty-creation power with a registry-bounded operand schema. \[Inference\] The bound (role-family act registry) is our compile-time discipline, not the document's text; an unbounded reading would be a boundary token instead. Both readings representable; interpretation multiplicity handled per D6/A6.

**4\. Layer K.** Reporting line: REF(`quality-manager`, `operations-director`, mode=`reports-to`). g7 as above; v3 \= the $50k VAL. **5\. TIX.** Role instances τₜ \= employment interval; p7-created duties each get their own ⟨τₜ, τ\_f⟩ at assignment. **6\. Source-map.** `c6,n6 → ⟦jd-qm : responsibilities : row⟧`; `p5,p6 → ⟦jd-qm : authorities : row⟧`; `c7 → ⟦jd-qm : competencies : row⟧`; `p7 → ⟦jd-qm : catch-all : sentence⟧`.

**Verdict: COMPILED.**

---

## Run 6 — KPI Definition: On-Time Logging Rate (synthesized; closes the D1 vector-7 loop)

**1\. Source text.** *KPI-SEC-03: On-time incident logging rate \= (incidents logged ≤ 24h ÷ total incidents) per quarter. Target: 95% of the threshold defined in Policy P-11 §4 \[which sets 100%\], unless waived in writing by the QC Director for a given quarter. Data source: incident register. Owner: Security Manager.*

**2\. Layer Σ.** `n7` \= NRM(`security-manager`, `org`, act=achieve(v4), \+, `mandatory`) — the ownership row is the KPI's bearer binding. **3\. Layer X.** `p8` \= PWR(`qc-director`, operand=⟨GRD suspending v4's target for quarter Q⟩, effect=`create`, exercise=`written-waiver`) — the waiver; its exercise transitions the quarter's target-instance to `suspended` (E), with TIX confining the waiver to Q. **4\. Layer K.** `v4` \= VAL(fn \= ratio(count(log-events: elapsed ≤ 24h, window=Q), count(incidents, window=Q)), unit=`%`, comparator=≥, target \= 0.95 × REF(`policy-p11-§4.threshold`)) — **the target is arithmetic over a REF**: the D1 falsification vector \#7 (normative reference embedded in a formula), compiling natively because VAL.target is a term, and REF is a T₁-readable sort. Evidence designation: REF(v4, `incident-register`, mode=`measures`). Dependency closure: v4 → P-11, v4 → Run 3's register — a three-artifact lattice; amend P-11 §4 and π₃ reports every affected quarter's verdict fork. **6\. Source-map.** `v4 → ⟦kpi-cat : SEC-03 : row⟧`; `p8 → ⟦kpi-cat : SEC-03-waiver : cell⟧`. **5\. TIX.** v4 instances per quarter (τ\_f \= Q); target forks with P-11's τₜ.

**Verdict: COMPILED.**

---

## 7\. Consolidated Result

| Run | Artifact class | Verdict | Boundary tokens | Strains | FEs |
| :---- | :---- | :---- | :---- | :---- | :---- |
| 1 | Statute (US, quoted) | COMPILED | 0 | 0 | 0 |
| 2 | ISO chapter | COMPILED-C | 1 (OT-1) | 0 | 0 |
| 3 | SOP (tri-source) | COMPILED | 0 | 0 | 0 |
| 4 | EPC/procurement clause | COMPILED | 0 | 1 (resolved) | 0 |
| 5 | JD | COMPILED | 0 | 0 | 0 |
| 6 | KPI formula | COMPILED | 0 | 0 | 0 |

**Falsification Events: zero. Kernel amendments: zero. T₁ amendments: zero.** Every layer received non-trivial content in ≥ 5 of 6 runs (Σ: 6/6; X: 5/6; K: 6/6; TIX: 6/6; P: 6/6) — the layers are load-bearing in practice, not only in destruction theory (D7 M2). The Run 4 strain is the exercise's most valuable entry: it demonstrates the Iron Rule *functioning* — a failed first encoding, a forbidden amendment identified and refused, a lawful reformulation found, and the audit trail preserved.

**Bounded completeness verdict: over the six benchmark excerpts, at the stated paraphrase and sampling limitations, ⟨B₁, T₁⟩ is complete with one conditional region and zero falsification events. Combined with D5, both held-out predictions of the program now stand confirmed at their tested depths.**

**Hypothesis status after D5/D8: SUPPORTED at Phase-0 scope** — pending only D9 (architecture comparison) and D10 (final recommendation) for the record, and subject to the standing falsification surface (D0 §12), which remains open by constitution. Residual risks for the record: full-text licensed corpora untested; paraphrase fidelity unaudited; adversarial legal review of the Run-4 reformulation outstanding.

*End of D8. Awaiting Agent 0 gate decision.*  
